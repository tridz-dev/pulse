// Stub API client - full implementation pending
export const apiClient = {
  get: async (url: string) => {
    console.warn('API client not fully implemented:', url);
    return null;
  },
  post: async (url: string, data?: unknown) => {
    console.warn('API client not fully implemented:', url, data);
    return null;
  },
  put: async (url: string, data?: unknown) => {
    console.warn('API client not fully implemented:', url, data);
    return null;
  },
  delete: async (url: string) => {
    console.warn('API client not fully implemented:', url);
    return null;
  },
};
